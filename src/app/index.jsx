import LoginScreen from '@/screens/LoginScreen';

/**
 * Entry route – renders the Login screen.
 * After successful login, navigate to /(tabs).
 */
export default function Index() {
  return <LoginScreen />;
}
